# 변환 데이터 정합성 점검 (yolo_dataset_9class_v2)

## 2026-09-04 정정: 두 변환 작업의 결과가 합쳐져 있다

**아래 2026-06-20 기록의 원인 추정과 삭제·재변환 권고는 최신 지침이 아니다.**
파일 수가 많다는 이유로 기존 train 디렉터리를 비우거나 파일을 삭제하지 않는다.
이전 기록은 train 804,421개와 `convert_v2`의 객체 라벨 518,728개만 비교해
약 44만 개를 중단·재실행 후 남은 파일로 추정했다. 그러나 실제로는 같은 날 별도
`convert_remainder`가 stride 샘플에 포함되지 않은 나머지 이미지를 추가했다.

| 실제 2026-06-12 로그 | 대상 페어 | 실패 | 빈 라벨 제외 | 저장 이미지 | 객체 라벨 합계 |
|---|---:|---:|---:|---:|---:|
| convert_v2 / train | 361,597 | 41 | 10 | 361,546 | 518,728 |
| convert_remainder / train | 442,925 | 35 | 15 | 442,875 | 513,381 |
| train 합계 | 804,522 | 76 | 25 | **804,421** | **1,032,109** |
| convert_v2 / val | 47,830 | 4 | 0 | 47,826 | 68,147 |

저장 이미지 수는 각 로그의 대상−실패−빈 라벨 제외로 계산했다.
`convert_remainder`의 마지막 실제 파일 수 804,421과 합계가 일치한다.
위 표는 **당시 실행 로그**이며 현재 전체 파일을 다시 집계한 결과는 아니다.
이 일치는 데이터셋 전체의 무중복·무누수·정답 정확성을 증명하지 않는다.

### 확인한 실행 설정과 변환 방식

2026-09-04 재조회 시 두 컨테이너 모두 exit 0/OOM false였다.

- `convert_v2`: imgsz 640, train cap 15,000, val cap 2,000, workers 12.
  2026-06-12 11:14:55~14:50:10 KST 실행.
- `convert_remainder`: imgsz 640, cap 15,000, workers 12, `--include_val` 미사용.
  같은 날 15:54:16~20:40:27 KST 실행.
- image ID는 둘 다
  `sha256:5e81d79be3ac54fb3dd1e8f65e44c4f2db0d154fb31674d8eef1fc729e3b6980`이다.
- 현재 남아 있는 두 converter의 핵심 변환은 BGR decode → 긴 변 최대 640,
  확대 없음 → `int()` 버림 크기/INTER_AREA → JPEG quality 90이다.
- 기본 결과는 `train_NNNNNNN`, 잔여 결과는 `train_rNNNNNNN`으로 이름이 분리된다.
  번호는 작업 목록의 인덱스이며 병렬 작업 완료 순서로 정해지지 않는다.

현재 converter 코드 SHA:

- convert_v2.py: `52dee1d692f979c352f32510a74a84ea6e8892c08a47e1d29b8922b31ff95c22`
- convert_remainder.py: `46cd95812d9e3fea0938d57a7e3ba48479dc897ca144a6e536c7e3cab8ae0c01`

현재 파일이 당시 실행 시점의 바이트와 같았다는 별도 attestation은 없다.
따라서 실행 설정과 번호는 원본 **후보 검색**에만 사용하고, 연결 확인은 실제 원본을
재변환한 JPEG SHA와 보호 legacy 이미지 SHA의 일치를 요구한다. 미일치는 다른
원본이라는 증명이 아니다. 현재 목록·파일·라이브러리 변화 가능성을 남긴다.

### 보존한 실제 증거

NAS의 다음 새 경로에 필요한 상태와 원문 로그를 읽기 전용 조회로 복사했다.
`/share/Container/operational_refresh_80bf78a_20260904_101000/legacy_history_20260904`

- `converter_container_state.txt` SHA
  `6bb2b748a8fbb2415844ce9afa468fa40c80b0806531505d75ae342370785f14`
- `convert_v2.log` SHA
  `36fe9e0eba908cfc87721b14e6ae33e0f114410700580bd8fdd0642f99a3804b`
- `convert_remainder.log` SHA
  `60216b1913d48e2e4b153e3e6e07958ba2fe472d010706ff8993e763a65d3db9`

### 현재 개선 작업에서의 사용 경계

보호 집합의 legacy 2,855개는 기본 train 1,255개/잔여 train_r 600개/val 1,000개다.
실제 SHA와 pHash는 이미 확인됐지만 원본 ID 연결은 별도 조사 중이다.
현재 원본 162,305장 집합은 단일 객체·직접촬영 위주이므로 legacy 전체가 그 집합에
들어 있다는 가정은 금지한다. 연결된 원본이 여러 개면 모두 보호하며, 미확인 항목은
미확인으로 남긴다. 과거 예측 재질·bbox·상태를 새 정답으로 복사하지 않는다.

새 학습 입력은 기존 디렉터리를 삭제하거나 재사용하지 않고, 원본 JSON/이미지 검증과
보호·교차 분할 중복 검사를 거친 새 불변 경로에 구성한다. 자세한 실측과 현재 작업은
`AIHUB_REPLAY_NEXT_STEP_20260904.md`를 따른다. 이 감사만으로 학습·blind·배포를 승인하지 않는다.

---

## 이전 2026-06-20 기록 (원인 추정 및 삭제 권고는 위 정정으로 대체)

> 메인 YOLO26m 학습용 변환 결과물 점검. 학습 **재개 전 해결 필요**.
> 점검일: 2026-06-20

---

## 1. 점검 결과

| 구분 | 이미지 | 라벨 파일 | 빈 라벨 | 객체 합(로그) | 객체/이미지 |
|------|--------|-----------|---------|---------------|-------------|
| **train** | 804,421 | 804,421 | **0** | 518,728 | 0.64 ⚠️ |
| **val** | 47,826 | 47,826 | — | 68,147 | 1.43 ✅ |

- 이미지 ↔ 라벨 파일 수 **정확히 일치** (짝 깨짐 없음)
- 빈 라벨 파일 **0개** → "객체 없는 배경 이미지" 오학습 우려는 **없음**
- val은 1.43 객체/이미지로 **정상**

## 2. 이상점

**train 파일 수(804,421)가 변환 로그의 객체 합(518,728)보다 많다.**

빈 라벨이 0인데 파일이 객체보다 많은 건 모순 → **변환 로그를 남긴 실행(재실행) 외의 파일이 디렉토리에 누적**됐다는 의미.

### 역산
- val 비율(1.43 객체/img)을 train에 적용 → 정상 파일 ≈ 518,728 ÷ 1.43 ≈ **364,000**
- 이는 재실행 변환량(361,597 페어)과 **거의 일치** ✅
- 잔여 = 804,421 − 364,000 ≈ **약 44만 파일이 이전 실행 잔여**

## 3. 원인 (추정)

- 종이 0쌍 문제로 **첫 실행 → 중단 → 재실행** 과정에서, 첫 실행 결과(`yolo_dataset_9class_v2`)를 `sudo rm` 했으나 **완전히 지워지지 않았거나**, 8일 경과 중 추가 실행이 누적된 것으로 보임.
- 두 실행의 파일 stem(`train_0000000…`)이 인덱스 범위가 달라 덮어쓰이지 않고 **잔여로 남음**.
- val은 영향 없음 (val 디렉토리는 깨끗하게 재생성됨).

## 4. 영향

- 빈 라벨이 아니므로 **치명적이진 않음** (모든 잔여 파일도 객체 보유)
- 그러나 ⚠️ **중복/train-val 누수 위험**: 잔여 파일이 재실행 파일과 같은 원본일 수 있어, 학습 시 중복 노출 → 성능 과대평가 가능

## 5. 조치 (메인 학습 재개 시)

**권장: train 디렉토리를 비우고 새 출력 폴더로 재변환** (가장 확실).
```bash
# 깨끗한 새 폴더로 재변환
docker run -d --name convert_v3 -v /share/Container:/app ultralytics/ultralytics:latest \
  python /app/convert_v2.py \
    --dataset_dir /app/ai_dataset/학습용_데이터 \
    --output_dir  /app/yolo_dataset_9class_v3 \   # ← 새 폴더
    --imgsz 640 --max_per_folder 15000 --val_max_per_folder 2000 --workers 32
```
- convert_v2.py에 이미 "기존 출력 폴더 존재 시 경고" 로직 있음 (L284-288)
- 또는 정밀 조사 후 잔여만 선별 삭제 (최대 인덱스 `ls train/images|sort|tail`로 잔여 범위 확인)

## 6. 미완 조사 (crop 추출 종료 후 진행)

- [ ] `ls train/images | sort | tail -1` — 최대 인덱스로 누적 범위 확정
- [ ] 중복 원본 여부 (동일 원본이 다른 stem으로 존재하는지)
- [ ] 재변환 vs 선별삭제 결정

> crop 추출이 디스크를 점유 중이라 804K 파일 정렬은 부하가 커 보류. crop 완료 후 진행.
