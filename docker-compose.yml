services:
  ai-server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./weights:/app/weights
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "3.0"
